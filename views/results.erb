
So, glad you want to do <%= result %>

<% first_number = p1.to_i %>
<% second_number = p2.to_i %>

<% 
if result == "addition"
	 final_result = addition(first_number, second_number) 
elsif result == "subtraction"
	 final_result = subtraction(first_number, second_number) 
elsif result == "multiplication"
		final_result = multiplication(first_number, second_number) 
elsif result == "division"
	final_result = division(first_number, second_number) 
else
	final_result = "This is not one of the choices"
end
%>

Your results are <%= final_result %>



